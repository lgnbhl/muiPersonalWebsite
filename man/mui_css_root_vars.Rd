% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{mui_css_root_vars}
\alias{mui_css_root_vars}
\title{The palette as CSS custom properties}
\usage{
mui_css_root_vars(config = mui_config())
}
\arguments{
\item{config}{The site configuration; see \code{\link[=mui_read_config]{mui_read_config()}}.}
}
\value{
A \verb{<style>} element, as a string.
}
\description{
The same colours \code{\link[=mui_theme]{mui_theme()}} gives the MUI half of the site, emitted as \verb{:root}
custom properties for the half the stylesheet still draws - the markdown body and the
honeycomb. Written into \verb{<head>} at build time so those paint in the right colours on the
first frame rather than after a stylesheet round-trip.
}
\details{
Both halves name colours by role (\code{--site-ink}, \code{--site-bg}, \code{--site-primary}) rather
than by hue, so a colour can be repointed in the configuration without touching CSS. The
\verb{--site-} prefix keeps these clear of \verb{--mui-palette-*}, which MUI owns in its
CSS-variables mode, and of whatever a site adds in its own stylesheet.
}
\seealso{
Other theme: 
\code{\link{mui_theme}()}
}
\concept{theme}
\keyword{internal}
