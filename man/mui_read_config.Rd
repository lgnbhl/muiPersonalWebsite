% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{mui_read_config}
\alias{mui_read_config}
\alias{mui_validate_config}
\title{Read a site configuration}
\usage{
mui_read_config(path = "mui.config.yml")

mui_validate_config(config, strict = FALSE)
}
\arguments{
\item{path}{Path to the YAML configuration file. A missing file is not an error: the
defaults alone are a valid configuration.}

\item{config}{A configuration list.}

\item{strict}{Also make the checks only a build can make: that there is at least one
section, and that every section's content file exists. Off by default, because
\code{\link[=mui_default_config]{mui_default_config()}} alone is a valid configuration but is not yet a site.}
}
\value{
A named list, validated by \code{\link[=mui_validate_config]{mui_validate_config()}}.
}
\description{
Reads \code{mui.config.yml} and merges it over \code{\link[=mui_default_config]{mui_default_config()}}. Keys the file does not
name keep their default, so a configuration only has to say what is particular to it.
}
\details{
The file is \code{mui.config.yml} and not \verb{_site.yml}, because rmarkdown defines a directory
holding \verb{_site.yml} to be an R Markdown website and would try to render this one.

\code{url} is the address the site is served at, and its path is the site's base path: a
site served at the root of a domain names the domain alone, and a GitHub Pages project
site names \verb{https://<user>.github.io/<repo>}, which prefixes every URL the build writes
with \verb{/<repo>}. An href a content file writes starting with \code{/} is a path on the host and
is not prefixed; name an asset by its filename to have it resolved under the base path.
}
\section{Security}{
If a \code{variants.R} file sits beside the configuration, reading it \strong{runs that file}: it
is \code{source()}d so the section variants it defines can be named in the YAML. Read and
build only sites whose files you trust, exactly as you would only \code{source()} a script you
trust. \code{\link[=mui_build_site]{mui_build_site()}} reads the configuration the same way.
}

\examples{
config <- mui_read_config(system.file("site", "mui.config.yml", package = "muiPersonalWebsite"))
config$title
}
