% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fallback.R
\name{mui_prerender_html}
\alias{mui_prerender_html}
\title{The landing page in plain HTML}
\usage{
mui_prerender_html(items, config = mui_config())
}
\arguments{
\item{items}{Items by section id, exactly as \code{\link[=mui_build_site]{mui_build_site()}} assembles them.}

\item{config}{The site configuration; see \code{\link[=mui_read_config]{mui_read_config()}}.}
}
\value{
A \verb{<div>}, as a string, or \code{""} when \code{prerender} is off.
}
\description{
The same hero and the same items the React tree renders, as a document a crawler, a reader
mode or a text browser can read. Written into the page ahead of the React container and
removed when React mounts, so it costs a reader with JavaScript nothing but is the whole
page to a reader without it.
}
\details{
It mirrors the \emph{content}, not the layout: one \verb{<section>} per band and one \verb{<article>} per
item, whatever variant the band is drawn with. Arrangement is what a plain document does
not need.
}
\seealso{
Other render: 
\code{\link{mui_boot_script}()},
\code{\link{mui_feed_xml}()},
\code{\link{mui_render_page}()},
\code{\link{mui_trim_payload}()}
}
\concept{render}
\keyword{internal}
