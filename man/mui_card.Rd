% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mui-card.R
\name{mui_card}
\alias{mui_card}
\alias{mui_card_grid}
\title{The shared card}
\usage{
mui_card(item)

mui_card_grid(items, columns = 3)
}
\arguments{
\item{item}{A card: \code{list(image, fit, title, description, primary = list(label, href), secondary = ...)} - which is exactly what an item in a \code{variant: cards} content file
says, so a content file feeds this directly with nothing in between.}

\item{items}{Cards, for the grid.}

\item{columns}{How many cards stand across a medium screen.}
}
\value{
A \code{muiMaterial} component.
}
\description{
Apps and packages are the same kind of thing: a picture, a sentence, somewhere to try it
and somewhere to read the code. They resolve to one shape and render through one card,
rather than two components that would drift apart.
}
\details{
The card is deliberately not wrapped in a \code{CardActionArea}: two destinations means two
links, and a Button inside a CardActionArea is an \verb{<a>} inside an \verb{<a>}.
}
\examples{
mui_card(list(title = "aniview", description = "Animate on scroll.",
               primary = list(label = "Docs", href = "/aniview/")))
}
\seealso{
Other home: 
\code{\link{mui_accordion}()},
\code{\link{mui_hero}()},
\code{\link{mui_hex_grid}()},
\code{\link{mui_hex_layout}()},
\code{\link{mui_home_page}()},
\code{\link{mui_list}()},
\code{\link{mui_prose}()},
\code{\link{mui_showcase}()}
}
\concept{home}
