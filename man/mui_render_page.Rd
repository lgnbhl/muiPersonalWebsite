% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{mui_render_page}
\alias{mui_render_page}
\title{Render a page to a standalone HTML file}
\usage{
mui_render_page(
  ui,
  page,
  out_dir,
  config = mui_config(),
  fallback = "",
  home = FALSE
)
}
\arguments{
\item{ui}{The page's tag tree.}

\item{page}{A page: its \code{out} path and its \code{url}, plus the front matter the \verb{<head>}
metadata is built from.}

\item{out_dir}{Directory to write into.}

\item{config}{The site configuration; see \code{\link[=mui_read_config]{mui_read_config()}}.}

\item{fallback}{The page's content as plain HTML, written into the document ahead of the
React tree and removed on mount - see \code{\link[=mui_prerender_html]{mui_prerender_html()}}. \code{""} writes none.}

\item{home}{Whether this page carries the bands the scroll spy watches; see
\code{\link[=mui_boot_script]{mui_boot_script()}}.}
}
\value{
The page's URL, invisibly.
}
\description{
\code{htmltools::save_html()} is deliberately not used: it cannot emit per-page \verb{<meta>} tags,
and it copies the full dependency tree next to every single page. Each dependency is
copied once into \verb{<out_dir>/_mui/libs} and referenced site-absolutely instead.
}
\seealso{
Other render: 
\code{\link{mui_boot_script}()},
\code{\link{mui_feed_xml}()},
\code{\link{mui_prerender_html}()},
\code{\link{mui_trim_payload}()}
}
\concept{render}
