% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scaffold.R
\name{mui_create_site}
\alias{mui_create_site}
\title{Create a new site}
\usage{
mui_create_site(
  path = ".",
  cname = NULL,
  starter = c("blank", "demo"),
  overwrite = FALSE,
  url = NULL
)
}
\arguments{
\item{path}{Directory to create the site in. Created if missing.}

\item{cname}{The custom domain to serve the site at, written into the configuration and
published as \code{CNAME}. Leave \code{NULL} for a site served at \verb{<user>.github.io}, with or
without a repository path, which needs none.}

\item{starter}{\code{"blank"} for placeholder words, \code{"demo"} for this author's real site.}

\item{overwrite}{Overwrite files that are already there. \code{FALSE} stops rather than
quietly replacing a configuration someone has edited.}

\item{url}{The address the site is served at, when it is not \verb{https://<cname>} - for a
project site, \verb{https://<user>.github.io/<repo>}. See \code{\link[=mui_read_config]{mui_read_config()}}.}
}
\value{
\code{path}, invisibly.
}
\description{
Writes a working site into \code{path}: a configuration, content files, an assets directory, a
404 page and a build script. Build it with \code{\link[=mui_build_site]{mui_build_site()}} into the \verb{docs/} tree GitHub
Pages serves, commit that, and switch Pages on for the \code{main} branch and the \verb{/docs}
folder. Then edit YAML: the blank starter has no R in it beyond the one-line \code{build.R},
and the demo starter adds only a \code{variants.R} holding the one band it draws itself.
}
\details{
Where the site is served decides what to pass. For a custom domain, give \code{cname}; the
\code{url} follows from it. For a repository named \verb{<user>.github.io}, give neither and set
\code{url} in \code{mui.config.yml} to \verb{https://<user>.github.io}. For a project site - any other
repository, served under \verb{https://<user>.github.io/<repo>} - give that as \code{url}: its path
becomes the site's base path, and every URL the build writes is prefixed with it.

Building a site runs its \code{variants.R}; see the security note in \code{\link[=mui_read_config]{mui_read_config()}}.

No CI workflow is scaffolded: how a site is published is the site's own business, and a
workflow shipped here would be one more file to delete for anyone publishing another
way.
}
\section{Which starter}{
\code{starter = "blank"}, the default, writes a site whose words are placeholders and whose
three sections hold one example item each. It is generated from \code{\link[=mui_default_config]{mui_default_config()}}
rather than stored, so it cannot fall out of step with the code, and the first thing it
asks you to do is write rather than delete.

\code{starter = "demo"} copies this package's own site instead - felixluginbuhl.com, the tree
under \code{inst/site}. It is a site that is really built and really published, so what it
shows cannot quietly rot. The trade is that the words in it
are somebody else's, and that its talks name slide decks too large to ship inside an R
package, so a first build of it warns about assets that are not there. Replace the words;
\code{hero}, \code{social}, \code{footer} and the content files are where the identity lives. The
writing, images and logos it copies are not covered by the package's MIT licence - see
\code{content/LICENSE-CONTENT.md} in the copy - and are there to be replaced, not republished.

What the demo starter does \emph{not} hand over is what would act as the author on your
behalf, invisibly from the page: there is no analytics id, the Twitter handle is commented
out, and the domain is replaced by yours - or, when you give neither \code{cname} nor \code{url},
by the \verb{https://example.com} placeholder the first build warns about.
}

\examples{
site <- file.path(tempdir(), "my-site")
mui_create_site(site)
list.files(site)
}
