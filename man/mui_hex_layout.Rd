% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hex-grid.R
\name{mui_hex_layout}
\alias{mui_hex_layout}
\title{Choose the honeycomb's shape}
\usage{
mui_hex_layout(n)
}
\arguments{
\item{n}{Number of cells: the packages, plus the one empty slot.}
}
\value{
A list: \code{rows} (cells per row), \code{box} (px), \code{aspect}, \code{cell_px}, \code{cell_f},
\code{gap_f}.
}
\description{
What is decided per build is the comb's \emph{shape} - cells to a row, and the rows that
follow from it. Its proportions depend on those two counts alone, since scaling the
cells scales both sides of the block, so the shape is chosen against a target aspect
ratio and the size follows from whichever budget binds.
}
\details{
Choosing by cell size instead - the fewest cells per row that fits a height cap - always
lands on the widest, shortest comb that will fit, which reads as a banner rather than as
paving.
}
\seealso{
Other home: 
\code{\link{mui_accordion}()},
\code{\link{mui_card}()},
\code{\link{mui_hero}()},
\code{\link{mui_hex_grid}()},
\code{\link{mui_home_page}()},
\code{\link{mui_list}()},
\code{\link{mui_prose}()},
\code{\link{mui_showcase}()}
}
\concept{home}
\keyword{internal}
