% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/payload.R
\name{mui_trim_payload}
\alias{mui_trim_payload}
\title{Trim the copied JavaScript}
\usage{
mui_trim_payload(dir, name, config = mui_config())
}
\arguments{
\item{dir}{The dependency's directory in the built tree.}

\item{name}{The dependency's name.}

\item{config}{The site configuration; see \code{\link[=mui_read_config]{mui_read_config()}}.}
}
\value{
The bytes saved, invisibly - \code{0} when nothing was rewritten.
}
\description{
Rewrites \code{shiny-react.js} in place, under \verb{_mui/libs}, replacing the whole of lodash with
the two functions the bundle actually imports from it. Roughly 470 KB of the built site's
1.4 MB, and the one saving available without a JavaScript toolchain: \code{shiny.react} itself
is what mounts every page and cannot be dropped, and the MUI bundle beside it is already
minified.
}
\details{
Every assumption is checked against the bundle in front of it, and any one of them failing
leaves the file untouched and reports why. \code{trim_payload: false} turns it off.
}
\seealso{
Other render: 
\code{\link{mui_boot_script}()},
\code{\link{mui_feed_xml}()},
\code{\link{mui_prerender_html}()},
\code{\link{mui_render_page}()}
}
\concept{render}
\keyword{internal}
