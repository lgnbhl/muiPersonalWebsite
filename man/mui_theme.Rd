% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{mui_theme}
\alias{mui_theme}
\title{The site's MUI theme}
\usage{
mui_theme(config = mui_config())
}
\arguments{
\item{config}{The site configuration; see \code{\link[=mui_read_config]{mui_read_config()}}.}
}
\value{
A theme list, ready for \code{muiMaterial::ThemeProvider()}.
}
\description{
The single theme every MUI component on the site renders under: palette, shape,
typography, and the component overrides that make a Card, an Accordion and an AppBar
look like this site rather than like stock Material Design.
}
\details{
Its colours are read from \code{config}, not declared here, because the half of the site that
MUI does not render needs the same ones - see \code{\link[=mui_css_root_vars]{mui_css_root_vars()}}.

\code{theme.mode: auto} emits \emph{both} schemes, as MUI \code{colorSchemes} under
\code{cssVariables$colorSchemeSelector = "media"}: MUI then writes one set of CSS variables per
scheme and switches them on \code{prefers-color-scheme}, with no toggle, no stored preference
and no script. The component overrides below name \verb{var(--site-*)} rather than a colour for
the same reason - a hex baked into a \code{styleOverride} would stay light in the dark scheme.
}
\examples{
theme <- mui_theme()
theme$colorSchemes$light$palette$primary$main
theme$typography$kicker
}
\seealso{
Other theme: 
\code{\link{mui_css_root_vars}()}
}
\concept{theme}
