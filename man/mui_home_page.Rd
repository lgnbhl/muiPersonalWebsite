% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/page-home.R
\name{mui_home_page}
\alias{mui_home_page}
\title{The home page}
\usage{
mui_home_page(items, config = mui_config())
}
\arguments{
\item{items}{Items by section id.}

\item{config}{The site configuration; see \code{\link[=mui_read_config]{mui_read_config()}}.}
}
\value{
A tag list.
}
\description{
The whole site, bar the 404: a \code{muiMaterial} hero over one \code{muiMaterial} band per section.
}
\details{
The bands are not named here. Each one is a \code{sections} entry in \code{mui.config.yml} - its id,
its kicker and title, and the \code{SECTION_VARIANTS} key that says how to draw it - so the
page's shape, order and words are configuration. The id it carries is the same one the
app bar anchors at and the hero's scroll cue points to, which is what lets a section be
renamed or added without touching R.

There is no \code{ThemeProvider} here: \code{\link[=mui_app_shell]{mui_app_shell()}} wraps the whole page in one, so the
hero and the bands render under the same theme as the chrome around them.
}
\seealso{
Other home: 
\code{\link{mui_accordion}()},
\code{\link{mui_card}()},
\code{\link{mui_hero}()},
\code{\link{mui_hex_grid}()},
\code{\link{mui_hex_layout}()},
\code{\link{mui_list}()},
\code{\link{mui_prose}()},
\code{\link{mui_showcase}()}
}
\concept{home}
\keyword{internal}
