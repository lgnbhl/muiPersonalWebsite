% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mui-app-shell.R
\name{mui_app_shell}
\alias{mui_app_shell}
\title{The page shell}
\usage{
mui_app_shell(body, home = FALSE, bare = FALSE, config = mui_config())
}
\arguments{
\item{body}{The page's content.}

\item{home}{Whether this page carries the bands the app bar's anchors point at. Only the
landing page does, and the anchor tabs are hidden everywhere else - a fragment cannot
reach a band that React has not rendered yet.}

\item{bare}{Drop the width cap from \verb{<main>}, for the home page whose bands run edge to
edge and set their own inner width. Every other page keeps the cap.}

\item{config}{The site configuration; see \code{\link[=mui_read_config]{mui_read_config()}}.}
}
\value{
A \code{muiMaterial} page.
}
\description{
Everything every page has: the app bar, the mobile drawer, \verb{<main>}, and the footer - all
of it inside one \code{ThemeProvider}, so every element on the page is a \code{muiMaterial}
component rendering under the site's theme.
}
\details{
One themed block rather than two: the chrome used to sit inside it and \verb{<main>} and the
footer outside, because plain HTML was what put the page's words in the source. Nothing
is outside it now - see the design notes on what \code{R/seo.R} carries instead.

No \code{useMaterialIconsOutlined}: the site draws its icons as inline SVG precisely so it
does not load an icon font.
}
\seealso{
Other shell: 
\code{\link{mui_app_bar}()},
\code{\link{mui_footer}()},
\code{\link{mui_page_head}()}
}
\concept{shell}
