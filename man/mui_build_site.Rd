% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build.R
\name{mui_build_site}
\alias{mui_build_site}
\title{Build the site}
\usage{
mui_build_site(
  out_dir = "dist",
  input = ".",
  config = NULL,
  clean = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{out_dir}{Directory to write the built site into. Created if missing. Existing files
are overwritten; whether the ones this build did not write are deleted is \code{clean}'s
business.}

\item{input}{Directory holding the site's content. The default is the working directory,
which is where the configuration and the content directories live.}

\item{config}{The site configuration. \code{NULL} - the default - reads \code{input}'s own
\code{mui.config.yml}, which is what makes \code{mui_build_site("docs", input = "inst/site")}
build the site under \code{inst/site}. A relative path passed here resolves the same way,
against \code{input} rather than against the caller's working directory.}

\item{clean}{Delete the pages a previous build wrote that this one did not. Renaming a
page otherwise leaves the old URL live and serving stale content, and nothing would
report it. Only the HTML named in the previous build's manifest is considered, so an
artefact the build does not own - a pre-rendered slide deck, a \code{copy_dirs} tree - is
never touched.}

\item{quiet}{Suppress the per-page build report.}
}
\value{
The built pages, invisibly: one list per page, each carrying its \code{url} and \code{out}.
}
\description{
Renders the site into \code{out_dir}: the landing page, the 404 page, the assets tree, and the
sitemap and robots files.
}
\details{
The site is one landing page. Each \code{sections} entry in the configuration names a YAML
content file, and the home page lays those out as bands - there are no section index
pages and no page per item, so nothing here has to be told what the sections are.

A content file naming an asset the built tree does not have is a dead button on the
page and nothing else would report it, so those are collected and warned about once,
by name, unless \code{quiet}.

Building a site runs its \code{variants.R}, if it has one - see the security note in
\code{\link[=mui_read_config]{mui_read_config()}}. Build only sites whose files you trust.
}
\examples{
\dontrun{
mui_build_site("dist")  # a scratch build
mui_build_site("docs")  # the tree GitHub Pages serves
}
}
