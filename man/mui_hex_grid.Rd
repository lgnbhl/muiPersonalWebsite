% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hex-grid.R
\name{mui_hex_grid}
\alias{mui_hex_grid}
\title{The package honeycomb}
\usage{
mui_hex_grid(items, config = mui_config(), label = "Projects")
}
\arguments{
\item{items}{Items of the hero section.}

\item{config}{The site configuration; see \code{\link[=mui_read_config]{mui_read_config()}}.}

\item{label}{What the comb is a comb of, for its \code{aria-label}.}
}
\value{
A \code{muiMaterial} component, or \code{NULL} when there is nothing to draw.
}
\description{
A comb of package stickers, plus exactly one empty slot: a dashed hex linking to GitHub,
saying the next package goes here.
}
\details{
The components are handed the answers rather than the algebra - \code{\link[=mui_hex_layout]{mui_hex_layout()}} decides
the shape and the stylesheet sizes the cells from the fractions it is given. The comb is
the one part of the site that keeps its stylesheet: clip-path cells, rows nested by
negative margins, a bloom-in fanned out along \code{--d} and an idle shimmer phased by \code{--i}
are what CSS is for, and restating them as \code{sx} would buy nothing. What is muiMaterial
here is the markup.

Which items these are is configuration: the comb is filled by the section that sets
\code{hero: true} in \code{mui.config.yml}, and an item with no image is skipped, since a sticker is
the one thing a cell cannot do without.
}
\seealso{
Other home: 
\code{\link{mui_accordion}()},
\code{\link{mui_card}()},
\code{\link{mui_hero}()},
\code{\link{mui_hex_layout}()},
\code{\link{mui_home_page}()},
\code{\link{mui_list}()},
\code{\link{mui_prose}()},
\code{\link{mui_showcase}()}
}
\concept{home}
\keyword{internal}
