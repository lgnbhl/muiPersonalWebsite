% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mui-list.R
\name{mui_list}
\alias{mui_list}
\alias{mui_timeline}
\title{A band of rows}
\usage{
mui_list(items)

mui_timeline(items)
}
\arguments{
\item{items}{Items from a section's content file.}
}
\value{
A \code{muiMaterial} component.
}
\description{
Titles, a line of context and the links out, one under the other. The variant for a
section whose items are neither pictures nor talks - publications, posts, a reading list -
where a grid of cards would be a grid of mostly-empty boxes.
}
\details{
\code{timeline} is the same rows with the year pulled into a column of its own and a rule down
the side, for items whose order is the point.

Neither knows what an item is. \code{title}, \code{description}, \code{body}, \code{date}, \code{primary},
\code{secondary} and \code{links} are read where they are there and skipped where they are not, so
any content file feeds either. \code{body} is markdown, for a description whose links belong in
the sentence rather than under it.
}
\examples{
mui_list(list(list(title = "A paper", description = "In a journal.")))
}
\seealso{
Other home: 
\code{\link{mui_accordion}()},
\code{\link{mui_card}()},
\code{\link{mui_hero}()},
\code{\link{mui_hex_grid}()},
\code{\link{mui_hex_layout}()},
\code{\link{mui_home_page}()},
\code{\link{mui_prose}()},
\code{\link{mui_showcase}()}
}
\concept{home}
