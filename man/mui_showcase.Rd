% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mui-showcase.R
\name{mui_showcase}
\alias{mui_showcase}
\title{The applications band}
\usage{
mui_showcase(items)
}
\arguments{
\item{items}{Application items: \code{slug}, \code{title}, \code{description}, \code{primary}, \code{secondary},
and a \code{shots} list of \code{src} and optional \code{caption}. An item with no \code{shots} falls back
to its \code{image}.}
}
\value{
A \code{muiMaterial} component.
}
\description{
A list of applications, and the screenshots of the selected one on a stage beside it. An
application is worth looking at for what it looks like, and a card in a grid shows one
strip of it.
}
\details{
Which app is showing is MUI's own state - \code{TabContext.static}, the way the talks accordion
and the mobile drawer are - so the band needs no server and no script. So is which
screenshot: each one is a \code{TabPanel} of a second \code{TabContext.static}, switched by the dots
under the stage. The stage is one fixed 16:10 shape for every app, so the band's height
does not move as you click down the list, and each screenshot is fitted into it rather
than cropped to it. Clicking a screenshot opens it full size in a \code{Dialog.triggerId}.
}
\examples{
mui_showcase(list(list(slug = "app", title = "An app",
                       shots = list(list(src = "/assets/app.png")))))
}
\seealso{
Other home: 
\code{\link{mui_accordion}()},
\code{\link{mui_card}()},
\code{\link{mui_hero}()},
\code{\link{mui_hex_grid}()},
\code{\link{mui_hex_layout}()},
\code{\link{mui_home_page}()},
\code{\link{mui_list}()},
\code{\link{mui_prose}()}
}
\concept{home}
